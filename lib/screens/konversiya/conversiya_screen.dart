import 'package:flutter/material.dart';

class ConversiyaScreen extends StatelessWidget {
  const ConversiyaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Konversiya"),
      ),
      body:
      Container(
        child: Center(
          child: 
          Text("Hello"),
        ),
      ),
    );
  }
}
